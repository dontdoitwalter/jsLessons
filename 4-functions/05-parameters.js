/*
PARAMETERS

Parameters are like empty storage boxes we can name whatever
*/

function pet(animal){
    console.log(`I have a ${animal} for a pet.`);
}

pet('cat');

function fullName(first, last){
    console.log(`Hello my name is ${first} ${last}.`);
}
console.log('Walter', "Van\ Hooser")